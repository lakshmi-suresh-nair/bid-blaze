{
  "devDependencies": {
    "gh-pages": "^6.1.1",
    "homepage": "https://github.com/lakshmi-suresh-nair/bid-blaze"
  },

  "scripts": {
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build",
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  }
}